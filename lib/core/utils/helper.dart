double defaultRadius = 10.0;
double defaultPadding = 15.0;

class Helper {
  double isMobile = 800;
  double isTablet = 1000;
  double isDesktop = 1000;

  Duration defaultDuration = const Duration(milliseconds: 200);
}
